using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RobotHat : MonoBehaviour
{
	//public Task robotHat;
	//public bool manager = false;

 //   private void OnTriggerEnter(Collider other)
 //   {
	//	// If the bot is given a box hat or hardhat, except manager must be given a box
 //       if (other.tag == "Box" || (other.tag == "Hardhat" && !manager))
	//	{
	//		if (!other.transform.GetComponent<Rigidbody>().isKinematic)
	//		{
	//			robotHat.Contribute();
	//		}
	//	}
 //   }
}
